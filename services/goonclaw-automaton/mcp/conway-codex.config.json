{
  "mcpServers": {
    "conway": {
      "command": "conway-terminal",
      "env": {
        "CONWAY_API_KEY": "${CONWAY_API_KEY}"
      }
    },
    "solana": {
      "command": "npx",
      "args": [
        "solana-mcp"
      ],
      "env": {
        "SOLANA_RPC_URL": "${SOLANA_RPC_URL}",
        "GOONCLAW_AGENT_WALLET_SECRET": "${GOONCLAW_AGENT_WALLET_SECRET}"
      }
    }
  }
}
